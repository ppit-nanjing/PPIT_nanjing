# Goal — Full Prototype Parity, End to End

> Working checklist. If you're picking this up cold (new session, `/loop`, or after a context reset): read [Progress & Handoff.md](./Progress%20&%20Handoff.md) first for how the app is wired (Neon/Drizzle/Auth.js/adminScope), then come back here. **Check items off in this file as you complete them and commit the updated file** — this doc is the durable memory across context resets, not the chat history.

## Why this doc exists

The app was built for real functionality first (every page reads/writes Neon, no mockups) rather than 1:1 visual fidelity to the 95 Stitch prototype files. That was the right call to get CRUD working everywhere, but it means a lot of pages are noticeably thinner than their prototype source. Evidence: `jobs_opportunities_expanded_ppit_nanjing/code.html` (prototype) is 364 lines with a hero search bar, a filter sidebar, and an inline Career Resources section; the built `/jobs` page is 76 lines with none of that. This doc's job is to close that gap systematically, screen by screen, using the actual prototype HTML as the source of truth for what's missing — not guessing.

**Ground rule:** when porting a section from a prototype `code.html`, bring over its structure/content/copy, but always express it with this project's real design tokens (`docs/Design System/`) and real Drizzle data (`docs/Entity Relationship Diagram.md`) — never hardcode fake data where a real query belongs, and never add a section that has no real backing data source without first adding the schema for it.

## Tier 0 — Routes that don't exist at all yet

These have zero implementation — not thin, literally missing.

- [x] **Peta Persebaran** — built 2026-08-15 as an SVG scatter plot (real lat/lng, equirectangular projection over China's bounding box) rather than a licensed map tile provider. Linked from `/organization/branches`.
- [x] **Review AD/ART Guidelines** — built 2026-08-15, informational (not a gate before another action - nothing in the app currently requires acknowledging this before proceeding, and no prototype evidence confirmed it should be a hard gate).
- [x] **Gallery Archive & Filters** — built 2026-08-15, filters by year (real `createdAt`) rather than "cabinet-period" (no such field/concept in schema, would've been fabricated).
- [x] **Submission History** — built 2026-08-15 at `/profile/submissions`, union of event_registrations + borrow_requests + job_applications for the signed-in user, sorted by date, linked from AccountMenu. **Event Submission Detail was deliberately NOT built as a separate `/profile/submissions/[id]` route** — each history item already links directly to its real entity page (`/events/[slug]`, `/inventory`, `/jobs/[id]`), and since the three submission kinds live in different tables with different ID spaces, a generic detail route would either need a redundant view or a kind-prefixed URL scheme for no real benefit over linking straight to the entity. Revisit only if a genuinely different detail view (e.g. showing the QR ticket inline) is wanted later.
- [x] **Inventory Audit Report (admin)** — built 2026-08-15 at `/console/inventory/audit-log`, mirrors the organization audit-log pattern.
- [x] **Notification Templates (admin)** — built 2026-08-17 at `/console/notifications`. `src/lib/notification-templates.ts` is a registry of the 7 notifications the app actually sends (event check-in, borrow approved/rejected, contribution approved/rejected, procurement approved/rejected), each with its trigger, allowed `{{variables}}`, and a default subject/body copied **verbatim** from the wording that was previously hardcoded at the call site — so an untouched install sends byte-identical messages to before. A `notification_templates` row overrides the default; deleting the row (the "Kembalikan ke bawaan" button) restores it, so there's no second copy of the defaults to keep in sync. New `createTemplatedNotification()` in `src/lib/notifications.ts` resolves row → default, renders `{{var}}`, and records `templateId` only when a real row backed the text. All 5 call sites rewired; `procurement`'s single `{{verb}}`-style message was split into separate approved/rejected templates so each outcome can be worded independently. Admin page is **full-tier only** — `"notifications"` is a new `AdminModule` deliberately kept out of `ASSIGNABLE_SCOPE_KEYS`, so `hasModuleAccess` only passes it via the `scope === "full"` short-circuit (same non-delegable treatment as users/organization/feedback), enforced in the server actions too, not just the page gate. Channel is fixed to `in_app`: there is still no email/push provider (see Progress & Handoff gap #3-adjacent note), and the page says so rather than offering a channel picker that would do nothing.
- [x] **Report Generator (admin)** — built 2026-08-15 at `/console/reports`: form to pick report type (`event_attendance`/`inventory_audit`/`sensus_summary`/`student_export`/`custom`) + department/date filters, streams CSV from `/api/console/generate-report`, logs each run to the `reports` table (`parameters_json`) and shows a "Riwayat Laporan" history list. Superseded and removed the old single-purpose `/api/console/export-students` route (folded into `student_export` type).

## Tier 1 — Built, but a real fidelity gap vs. the prototype (priority order)

For each: prototype line count vs. built line count, and what's concretely missing. Port section-by-section from the named prototype file.

1. [x] **Homepage** (`/`) — Latest Events + Latest News sections added (2026-08-15), ported from the prototype's card layout, real Drizzle queries, only render once content exists. Partners/Networks section intentionally skipped (no `partners` table, no real institution data - would require org input, see Tier 3-style note).
2. [x] **Jobs listing** (`/jobs`) — search (real ilike query), type + location filters (real query-param toggles, locations derived from actual data not hardcoded), relative "posted X ago" (added `jobPostings.createdAt`), inline Career Resources section. Done 2026-08-15.
3. [x] **Sensus Profile** (`/sensus`) — done 2026-08-15. Added real per-step progress save (`saveSensusStep`, fires on "Selanjutnya" so a step isn't lost before final submit — implements the "simpan progres per-langkah" note in `docs/Sensus Profile Flow.md`), a saved-at indicator, select inputs + field hints ported from the prototype (Jenis Kelamin, Jenjang, Sumber Pembiayaan), and a persistent "Perhatian" status alert for incomplete profiles (previously only shown via `returnTo`). **Not ported:** the prototype's Nomor Paspor, Masa Berlaku Paspor, Asal Provinsi, Status Mahasiswa, Tahun Masuk/Perkiraan Lulus, and Kartu Mahasiswa upload fields — none of these are in `docs/Data Dictionary.md` § SENSUS_PROFILE or `docs/Sensus Profile Flow.md`'s field list, both of which already match the built 11-field form. Since master_edition is one of 6 divergent variants and those two docs already documented a deliberate, narrower field set as canonical, adding passport/ID-photo collection now would be inventing scope beyond what's established, not closing a fidelity gap. Revisit only if the org confirms these fields are actually wanted.
4. [x] **About** (`/about`) — done 2026-08-15. Bento-style Visi/Misi (highlighted Vision card + icon Mission cards), real-fact badge row (founding date, location), and a new "Wilayah Cakupan" card section for the satellite cities/ranting orgs (previously buried in one sentence). **Not ported:** fabricated stat badges ("EST. 2010"/"500+ MEMBERS" — contradict the real 2008 founding date or are unconfirmed), the BPH photo grid (no real department-member data seeded yet — revisit at Tier 1 #14 once members exist), stock photography (hotlinking policy), and an unconfirmed "recognized by the Indonesian Embassy" claim.
5. [x] **Events listing** (`/events`) — done 2026-08-15. Category filter tabs (query-param, derived from real `event.category` values in use, not the prototype's hardcoded Academic/Culture/Sports), and a "Featured Event" hero card for the soonest real upcoming published event (computed from `startAt` vs now, no fabricated "featured" schema flag), plus a "SELESAI" badge/grayscale treatment for past events. No published events exist in the live DB yet, so only the empty state was exercised live — featured/grid rendering verified by code review against the already-proven `/jobs` filter pattern.
6. [x] **Event Detail** (`/events/[slug]`) — done 2026-08-15. Added `events.agenda` (freeform schedule, admin-editable, pushed to Neon) rendered as a timeline, a sticky info sidebar with a real "Sisa N slot" remaining-capacity figure, `registrationDeadline` now surfaced and actually enforced (was already in schema, unused), a gallery snippet from the event's real linked `gallery_albums`/`gallery_photos` (not stock photos), and a related-events section. Also wired `coverImageUrl`/`registrationDeadline`/`agenda` into the admin create/edit forms since they had no way to be set before. **Not ported:** the prototype's single fabricated example event (fake venue address, fake spots-remaining count, stock photography) — zero events exist in the live DB, so only empty/list states were exercised live. Speaker/organizer info skipped as speculative (Goal.md only said "likely" needed) — would require a department-picker addition to the admin form for uncertain payoff.
7. [x] **Comprehensive Career Center** (`/career`) — done 2026-08-15. Job cards now show a type badge, location, and relative post time; guide cards get an icon box and a real content-derived excerpt (Goal.md Tier 1 #7). **Not ported:** the prototype's full job-search/filter bar (redundant with `/jobs`, already real per Tier 1 #2), fabricated "5,000+ Anggota / 50+ Mitra" employer-partnership stats and proposal-download CTA (no real partnership program exists), and testimonials/alumni spotlight (no real data — Goal.md's own note only said "possibly").
8. [x] **Edit Profile** (`/profile`) — built 2026-08-15. Real form for `users.phone`/`wechatId` (account-level contact), plus a status card linking to `/sensus` for academic/demographic fields rather than duplicating them into a second form. Deliberate scope decision, not a full 1:1 field-for-field port of the prototype.
9. [x] **Join Mentorship Program** (`/career/mentorship`) — done 2026-08-15. Confirmed by reading the actual prototype HTML: it's not a mentor-browsing UI (no mentor directory/table exists anywhere), it's a fancier 3-step *application* form. Ported: two-column layout with a real "Kenapa Ikut?" benefit panel, and split the form into 2 steps (same 3 real schema fields — preferredField/background/motivation — just re-paced, preferredField upgraded to a real industry select). **Not ported:** the prototype's personal-info/academic-details steps (name, email, WeChat, university, degree, grad year, major) since these duplicate the account (Google OAuth) or the canonical Sensus profile — same call already made on Profile (Tier 1 #8); the stock photo + pull-quote (hotlinking policy); and a "primary goal" radio (no schema column, motivation already covers it free-text).
10. [x] **Career Guide article** (`/career/guide/[slug]`) — done 2026-08-15. Two-column layout with a sticky sidebar: real related-articles query (same category) and a computed read-time estimate from actual word count. The "mock interview" CTA was repurposed to link to the real Mentorship application flow rather than a fake booking system (no scheduling feature/table exists). **Not ported:** fabricated downloadable resources (resume template, algorithm question list, vocabulary list — none of these files exist) and the stock interview photo. No real guide articles exist in the live DB yet, so this was verified by code review only.
11. [x] **Job Detail** (`/jobs/[id]`) — done 2026-08-15. Banner header with type badge + relative post time (createdAt was already there, just never surfaced), sticky company card, and a real "Lowongan Serupa" similar-jobs section (same type/location). **Not ported:** the prototype's fabricated single example job (fake company logo, salary, employee count, website, HQ address) and its "Save Job" button — `jobPostings` has no salary/logo/size/website columns, and adding them for one hypothetical feature would be unconfirmed scope; the company card only shows the two real fields it has. No job postings exist in the live DB yet, so verified by code review only.
12. [x] **News listing** (`/news`) — done 2026-08-15. Added `news_articles.category` (freeform, admin-editable, same pattern as `events.category`, pushed to Neon) backing real category filter tabs, plus a featured hero card for the most recent published article (no fabricated "featured" flag). **Not ported:** the prototype's fabricated example articles/photos and invented category labels. No articles exist in the live DB yet, so verified by code review against the proven `/events` pattern rather than live data.
13. [x] **Terms** (`/terms`) — done 2026-08-15. Checked the prototype as asked: its actual rendered body content is generic Privacy-Policy boilerplate (placeholder "billing address"/"student ID" categories that don't fit this org) — the "Terms of Service" sidebar link has no corresponding content section in the file at all, so there was no real Terms content to port. The existing Keanggotaan/Data Sensus/Peminjaman Inventaris sections are already specific and accurate to this app, so content stayed as-is; only the legal-document sidebar navigator structure was worth porting (new `LegalNav` component, added to both `/terms` and `/privacy`). Skipped the prototype's "Last Updated: October 24, 2023" badge — no real update-tracking exists for these static pages and inventing a date would be dishonest.
14. [x] **Organization Structure** (`/organization`) — done 2026-08-15. Each department/division card now shows real assigned members (Google profile photo, name, position) via a real `departmentMembers`+`users` query; omitted entirely for divisions with no members (honest empty state, no placeholder). **Not ported:** the prototype's fabricated BPH/department-head names and stock headshots, and its generic English department taxonomy that doesn't match this org's real structure. Verified live: exactly one real member exists (the site owner, Divisi Teknologi/Developer) and renders correctly with their real photo.
15. [x] **Inventory catalog** (`/inventory`) — done 2026-08-15. Real search + category filter pills (same pattern as /jobs, /events, /news), and cards now surface `location`/`description` - both real columns that existed but were never shown. **Not ported:** fabricated items (Canon EOS R6, JBL PartyBox) and specific fake room locations; multi-photo galleries also skipped since `inventoryItems` only has a single `imageUrl` column, not an array. No items exist in the live DB yet, so verified via the empty state only.
16. [x] **Apply to Borrow** (`/inventory/[id]/borrow`) — done 2026-08-15. Converted to a 2-step `BorrowRequestForm` (same real fields: quantity/dates/purpose), added an item-location info note (real `location` column, was unused) and a required liability-agreement checkbox. **Not ported:** the prototype's Identity Verification step (name/student ID/ID upload — redundant with Google OAuth + the existing `requireCompletedSensus` gate) and its "Select Pickup Location" radio with fabricated specific campus locations (only one real location per item exists in schema).
17. [x] **AD/ART** (`/organization/ad-art`) — done 2026-08-15, with a deliberate scope call. Surfaced real `doc.title`/`doc.publishedAt` (previously unused) and added an inline PDF iframe preview. **Declined to port** the prototype's Mukadimah/BAB I/BAB II Pasal-by-Pasal text: it's Stitch-generated placeholder constitutional text, not PPIT Nanjing's real AD/ART, and presenting invented legal/founding-document text as genuine would be a more serious fabrication than anything else in this pass — no real AD/ART text exists anywhere in this project to port faithfully. `organization_documents.fileUrl` (admin-uploaded PDF) is the correct real source once uploaded. The existing `/organization/ad-art/review` page (prior session) already covers the "readable content" need with clearly-labeled generic guideline summaries, not verbatim fabricated legal text — that framing difference is why it was acceptable and literal Pasal text isn't.
18. [x] **Regional Branches** (`/organization/branches`) — done 2026-08-15. Bordered per-region cards, and `memberCount`/`contactInfo` now render when set (they weren't surfaced before). None of the 9 seeded branches have real values for these yet, so visually unchanged until an admin adds them (no admin UI exists for this table, out of scope here). **Not ported:** fabricated per-branch member counts and the "24 active provincial branches" claim (real: 9 of 32 confirmed) and stock map hero image — Peta Persebaran already exists as a real SVG scatter plot.
19. [x] **Login** (`/login`) — done 2026-08-15. Ported the card treatment (top accent bar, atmospheric blurred background, border/shadow) and a "Kembali ke Beranda" link. The prototype's email/password fields didn't apply when this was Google-OAuth-only; **Phase 2 (2026-08-15) added a real email/password (Credentials) sign-in form beside the Google button, plus a `/signup` page** — so the login screen now matches the prototype's email/password + SSO shape, but backed by a real bcrypt-hashed `users.passwordHash` and JWT sessions.

## Tier 2 — Functional-equivalent but different UX than the prototype envisioned (lower priority, defensible)

These aren't "missing" so much as "solved differently" — revisit only after Tier 0/1:

- **Add New Department / Edit Department / Manage Members / Reorder Hierarchy** — prototype had 4 separate admin screens; built version does all of this inline within `/console/organization` (collapsible forms + up/down buttons instead of drag-and-drop). Functionally complete, simpler UX.
- **Create New Event** — prototype had a dedicated `/admin/events/new` screen; built version uses a collapsible inline form on `/console/events`. Functionally complete.
- **Add New User** — admin manual-create flow intentionally not built; accounts are now created two ways without admin involvement: self-service email/password sign-up at `/signup` (Phase 2) and Google OAuth. Role/department assignment for new accounts is still done in `/console/users` after first sign-in. Leave as-is.
- **New Entry Selection modal** — prototype had a unified "what do you want to create?" picker; built version has each module's own inline add form instead. Cosmetic difference only.
- **7 admin Guide pages + Help & Documentation** — prototype had 8 separate screens; built version folds all guide content into `/console/docs` as `help_articles` rows (5 seeded so far, one per real module) rather than one static page per guide topic. Functionally superior (admin-editable) but not a 1:1 visual port — leave as-is.

## Tier 3 — Schema exists, no UI at all (future work, not urgent)

- `notifications` — **in-app notification bell built in Phase 2 (2026-08-15)**: a nav `NotificationBell` + `/notifications` page, backed by `src/lib/notifications.ts`, triggered on real admin status changes (borrow request approve/reject, event check-in). Scoped down to existing data, no email sending (no email provider available — `notification_templates` is still unused, and no prototype screen strictly required notifications). Email verification for sign-up was also intentionally skipped (no sending infra).

## Advanced Features (2026-08-15)

Built per the Advanced Features Build Spec (Obsidian vault). All code committed; the only outstanding item is a **live runtime test of §6.1 invited-account linking** (needs real Google OAuth sign-in + `BLOB_READ_WRITE_TOKEN` on Vercel — neither available in local dev, uploads currently return 503).

- [x] **§1 ImageUploadCropper** — `react-easy-crop`-based, controlled (`value`/`onValueChange`) + uncontrolled (`name`+hidden input) modes; `folder` whitelisted (`sensus`/`avatar`/`resume`/content/admin folders). Used by Sensus photo + Profile avatar.
- [x] **§2 Sensus mandatory photo** — `photoUrl` required on profile submit (returns `photo_required` otherwise); wizard wired to the cropper.
- [x] **§3 Profile avatar + 5 social links** — avatar cropper + LinkedIn/Instagram/GitHub/Spotify/TikTok inputs persisted via `updateProfile`; organization page renders generic social icons.
- [x] **§4a Contributions** — `itemContributions` table + enums; `createContribution`/`reviewContribution`; member `/inventory/contribute` form + admin `ContributionReview`.
- [x] **§4b Procurement** — `procurementRequests` table + enums; `createProcurementRequest`/`reviewProcurement`; member `/inventory/request-new` form + admin `ProcurementReview`.
- [x] **§4c External loans** — `externalLoans` table + `lent_external`/`returned_external` audit actions; `recordExternalLoan`/`returnExternalLoan`; admin `ExternalLoanManager`.
- [x] **§5 Inventory condition** — `inventoryConditionEnum` expanded to 5 values (new/good/fair/damaged/retired), pushed to Neon.
- [x] **§6.1 User invite + admin claim** — **koreksi 2026-08-21: baris ini setengah salah selama ini.** `createInvitedUser` **tidak pernah ada** — dicari di seluruh `src/`, nihil. Yang memang ada dan benar adalah dua sisi KLAIM-nya: `signUpWithPassword` mengklaim akun ber-status `invited` lewat jalur kata sandi, dan callback `signIn` di `src/auth.ts` menautkan sign-in Google ke akun undangan sebelum pemeriksaan adapter sendiri. Tapi karena **tidak ada satu pun kode yang bisa MEMBUAT baris ber-status `invited`**, kedua jalur itu tidak pernah bisa dijangkau — kode mati sejak ditulis. Bagian yang hilang baru dibangun 2026-08-21: `inviteUsers` di `src/app/actions/admin-users.ts` + formulir tempel-massal di `/console/users`. Massal sejak awal karena bentuk kebutuhannya memang begitu — satu kepanitiaan acara bisa berisi 30 orang yang belum punya akun, dan mereka harus ada dulu sebelum bisa ditugaskan ke divisi atau diberi sertifikat. Belum ada email undangan otomatis (tidak ada penyedia email, lihat gap #3).
- [x] **Responsive (Responsive Design Build Spec)** — `--spacing-container-padding` is now `clamp(1rem,4vw,1.5rem)`; `console-sidebar` is a desktop rail + mobile drawer (layout is `flex-col md:flex-row`); the three admin tables are wrapped in `overflow-x-auto` so they scroll on narrow screens instead of overflowing.

Remaining: live test of §6.1 linking; full 8-width browser pass not possible in this environment — applied the spec's two concrete code fixes (gutter clamp + sidebar) plus table-scroll wrappers.

## Process notes for whoever/whatever works through this

- Build, `git add` **one file at a time**, commit with a message explaining the *why* (matches the rest of this repo's history) — not batched commits.
- Run `npm run build` before each commit batch — this repo's `npm run build` catches real compile errors but does **not** catch schema-drift or auth-adapter bugs (see the two lessons documented in Progress & Handoff.md from 2026-08-15) — those only surface at actual runtime.
- After finishing an item, check its box in this file and commit the doc update alongside the code.
- If a prototype section references data with no corresponding table/column yet, add the schema first (`src/db/schema.ts` + `npx drizzle-kit push --force` immediately, don't batch schema changes).
- Don't invent organizational facts (branch names, real people, real events) — where the prototype shows placeholder/example content, either leave the real page's empty-state honest or use genuinely-known facts from `docs/Overview.md` and the guidebook, same standard applied throughout this project so far.
